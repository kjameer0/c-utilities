# Onboard utility toolkit

In this repo there are tools that can be used on MacOS as building blocks to simplify certain types of workflows.

__Examples__:

1. Monitoring a file for changes

## Architecture

Every script in this file should be independent and run solely with a core library. That core library will provide common behavior for every other tool to build off of.

